import { css } from '@emotion/react';

export const chartPageCss = {
  fileInput: css({
    position: 'absolute',
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    height: 32,
    overflow: 'hidden',
    opacity: 0,
  }),
};
